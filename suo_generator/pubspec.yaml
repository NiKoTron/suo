name: suo_generator
description: A starting point for Dart libraries or applications.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  build: ^1.2.2
  source_gen: ^0.9.5
  code_builder: ^3.3.0
  path: ^1.6.0

  suo:
    path: ../suo

dev_dependencies:
  build_test: ^1.0.0
  build_runner: ^1.9.0
  test: ^1.6.0
